# Fail loudly rather than silently resolving a phantom dependency.
# Every package must declare what it imports.
strict-peer-dependencies=true
auto-install-peers=false

# Keep node_modules non-hoisted so undeclared imports break immediately
# instead of working by accident and breaking on publish.
shamefully-hoist=false

# Reproducible installs in CI.
prefer-frozen-lockfile=true
