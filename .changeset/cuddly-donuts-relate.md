---
"@keepcv/schema": minor
---

Add the shared primitive vocabulary (`uuid`, `timestamp`, `partialDate`,
`contentHash`, `richText`), the versioned export document with its forward
migration registry, and the JSON Schema emitted from it.
